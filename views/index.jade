extends layout

block content
    #content
        - if(message !== 'undefined')
            p #{message.error}
            p #{message.success}
        #app!=html
        script.
            __DATA__ = !{data}
    script(src='/js.js')